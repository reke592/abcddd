----
+ deduction payment can be excluded in payroll report
+ class destructor to unsubscribe event listeners
+ NHRepositoryBase generic methods: Find<TOther> FindALL<TOther>
----
+ make logic for Deduction so it can be temporary or permanent
----

... fix n+1 queries
    we can use stateless session bulk insert

... add domain model authentication
    https://auth0.com/docs/tokens/jwt-claims
    iss (issuer): Issuer of the JWT
    sub (subject): Subject of the JWT (the user)
    aud (audience): Recipient for which the JWT is intended
    exp (expiration time): Time after which the JWT expires
    nbf (not before time): Time before which the JWT must not be accepted for processing
    iat (issued at time): Time at which the JWT was issued; can be used to determine age of the JWT
    jti (JWT ID): Unique identifier; can be used to prevent the JWT from being replayed (allows a token to be used only once)

... result caching
    https://nhibernate.info/doc/nhibernate-reference/caches.html#NHibernate.Caches.CoreMemoryCache

... add webapi infrastracture
... create minimal web UI
... how about we support monetary value convertion?

Create unit test
Create application and domain service implementations
