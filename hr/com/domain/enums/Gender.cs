namespace hr.com.domain.enums {
    // we use the Enum.TryParse<T>(), 0 means not found in enum
    public enum Gender {
        MALE = 1,
        FEMALE,
        TRANS_MALE,
        TRANS_FEMALE
    }
}